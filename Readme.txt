To compile run cmake, default settings should be fine.

All libraries are included as source in dependencies/ and 
compile with the engine/game so there is nothing else you need.
The default cmake settings should be fine.

Set working directory to bin/ (data folder is in there).